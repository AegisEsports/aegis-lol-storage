# Dockerfile.dev
# Development image: mounts source, installs all deps, and runs with a TS executor

FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Expose dev port
EXPOSE 3000
CMD ["npm", "run", "dev"]
